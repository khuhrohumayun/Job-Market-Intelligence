# Local development: zero setup. The database file is created automatically.
DATABASE_URL=sqlite:///data/job_market.db
# MySQL (after `docker compose up -d mysql`):
# DATABASE_URL=mysql+pymysql://jobintel:change_me@localhost:3306/job_market
LOG_LEVEL=INFO
REQUEST_TIMEOUT_SECONDS=20
SCRAPER_DELAY_SECONDS=1.5
USER_AGENT=PakistanJobMarketIntelligence/1.0 (portfolio project; contact: you@example.com)
